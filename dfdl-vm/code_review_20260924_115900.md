# Code Review - 2026-09-24 11:59:00

## Scope & Objective
- **Objective**: Fix remaining failing test in Section 02 (`runtimeSdeWithRestriction_1`) without causing regressions in Section 05 or unit tests.
- **Result**:
  - Section 02: 96 / 96 passed (100%).
  - Section 05: 811 / 811 passed (100%).
  - Unit Tests (`cargo test --lib`): 167 / 167 passed (100%).
  - Clippy (`cargo clippy --all-targets`): 0 errors, 0 warnings.

## Domain & Safety Review

### 1. DFDL Specification & Bit Order Alignment (Domain Expertise)
- **Finding**: According to the DFDL specification, changing `dfdl:bitOrder` between sequence elements off a byte boundary (i.e. when current bit position is not a multiple of 8) is classified as a **Runtime Schema Definition Error (RSDE)**, evaluated during parse/unparse execution when position is unaligned.
- **Fix**: Removed static `SchemaError` compile-time rejection in `src/ir/builder/validate.rs`. Added `seq_bit_order` tracking and `check_bit_order_change` in `src/vm/encoder.rs` alongside existing `runtime_check_bit_order_change` in `src/vm/decoder/sequence.rs`.
- **Validation**:
  - `section02/validation_errors/Validation.tdml::runtimeSdeWithRestriction_1` compiles successfully and triggers runtime RSDE as required by TDML.
  - `section05/simple_types/BitOrder.tdml::unparse:bitOrderChangeInvalid2Unparser` properly triggers unparse RSDE when `bitOrder` changes off a byte boundary during encoding.

### 2. VM Execution & Safety (Development & Type System Expertise)
- **Safety**: Verified that cell-based `seq_bit_order` mutations in `Encoder` maintain strict `no-panic` invariants and handle sub-byte alignment with `.is_multiple_of(8)`.
- **Memory & Performance**: Minimal overhead (single `Cell<Option<BitOrder>>` on `Encoder`). No extra allocations.

### 3. Verification & Code Quality
- **Unit Tests**: 167 / 167 passed.
- **Section 02 Test Suite**: 96 / 96 passed.
- **Section 05 Test Suite**: 811 / 811 passed.
- **Clippy**: Clean (0 warnings in workspace code).
